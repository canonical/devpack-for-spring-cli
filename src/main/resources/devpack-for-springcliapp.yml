spring:
  main:
    web-application-type: none
    banner-mode: off
  shell:
    interactive:
      enabled: true
    config:
      location: "{userconfig}/springcli"
    history:
      name: spring-cli-history.log
    command:
      completion:
        root-command: devpack-for-spring
      version:
        show-git-short-commit-id: true
devpack-for-spring:
    cli:

## disable/enable console logging
logging:
  pattern:
    console:
## log debug from a cli
  # file:
  #   name: shell.log
  # level:
  #   root: DEBUG
  #   org:
  #     springframework:
  #       cli: DEBUG
  #       shell: DEBUG
